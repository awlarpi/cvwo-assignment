version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: "postgres://postgres:5555@localhost:5432/postgres?sslmode=disable"
    gen:
      go:
        out: "db"
        sql_package: "pgx/v5"
